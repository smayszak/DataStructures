using NUnit.Framework;
using System;
using DataStructures.RedBlackTree;

namespace BuilderTests
{
	[TestFixture ()]
	public class RedBlackBuidlerTests
	{
		/*
		[Test ()]
		public void HeadNodeIsBlack ()
		{
			var ints = new int[]{1, 2};
			var head = Factory<int>.BuildFor (ints);
			Assert.IsTrue (head.Black);
		}

		[Test ()]
		public void TwoNodeHasGreatestAtRoot ()
		{
			var ints = new int[]{1, 2};
			var head = Factory<int>.BuildFor (ints);
			Assert.IsTrue (head.Data == 2);
		}

		[Test ()]
		public void TwoNodeHasGreatestAtRootUnsorted ()
		{
			var ints = new int[]{2, 1};
			var head = Factory<int>.BuildFor (ints);
			Assert.IsTrue (head.Data == 2);
		}

		[Test ()]
		public void FourNodesMaintainsRbTProperties ()
		{
			var ints = new int[]{2, 1, 3, 4};
			var head = Factory<int>.BuildFor (ints);
			Assert.IsTrue (head.Data == 3);
			Assert.IsTrue (head.Black);
			Assert.IsTrue (head.Left.Data == 2);
			Assert.IsFalse (head.Left.Black);
			Assert.IsTrue (head.Left.Left.Data == 1);
			Assert.IsTrue(head.Left.Left.Black);
			Assert.IsTrue (head.Right.Data == 4);
			Assert.IsTrue (head.Right.Black);
		}
		*/
	}
}

